Tu tarea es analizar el siguiente documento legal y devolver UN ÚNICO objeto JSON que cumpla EXACTAMENTE con el JSON Schema provisto más abajo.

- Devuelve SOLO JSON válido (sin backticks, sin Markdown, sin texto antes o después).
- No copies el texto del documento tal cual; tu misión es rellenar el schema con la información que el texto contenga.
- Mantén los nombres de claves exactamente como aparecen en el schema.
- Si no hay información para un campo, usa:
  - null para valores únicos,
  - [] para listas,
  - "" para textos vacíos.
- Todos los textos deben estar en español.
- No elimines ni cambies la estructura ni los tipos de datos.
- Asegúrate de que el JSON resultante sea parseable (comience con { y termine con }).